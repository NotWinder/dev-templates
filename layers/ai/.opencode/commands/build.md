---
description: "Implement the current in-progress task"
agent: "builder"
---

Implement the task currently in `tasks/in-progress/`.

Follow your full implementation workflow: read `AGENTS.md`, run `validate.sh`, implement the task, run tests and lint, append a completion summary, move the task to `tasks/done/`, and update `docs/context.md`.
